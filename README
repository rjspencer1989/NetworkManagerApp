Network Manager is an application that provides an overview of a wireless network from the point of view of the router. 
It is intended for use by home users, as its feature set is too limited for use in a business environment with a large
network.